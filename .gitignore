# 运行时数据（不进 git）
memory_quality.json
fts_cache.json
dreaming_doubt_list.json
pattern_cache.json
agent_errors.json
memory_vars.json
structured_persona.json

# 二进制 / 编译产物
glibc_shim.so
__pycache__/
*.pyc

# 环境变量（有密钥）
.env

# 归档
archive/

# 系统文件
=2.0.0
